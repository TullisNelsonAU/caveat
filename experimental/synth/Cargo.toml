[package]
name = "synth"
version = "0.0.0"          # experimental: builds two-sided-GT test binaries
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
publish = false

[[bin]]
name = "synth"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
evalkit.workspace = true
