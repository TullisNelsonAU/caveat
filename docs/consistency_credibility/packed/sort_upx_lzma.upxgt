# Format-exact GT for sort_upx_lzma — derived from UPX's own b_info chain (NOT a disassembler).
# 3 b_info blocks; p_filesize=282348.
# field       label     vaddr_start   vaddr_end     file_start  file_end
exec_segment  -         0x800000      0x80a9db      0x0       0xa9db
compressed    NEGATIVE  0x800188      0x809891      0x188       0x9891  provable data (38665 B)
