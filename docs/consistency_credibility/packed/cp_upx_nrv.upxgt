# Format-exact GT for cp_upx_nrv — derived from UPX's own b_info chain (NOT a disassembler).
# 3 b_info blocks; p_filesize=344665.
# field       label     vaddr_start   vaddr_end     file_start  file_end
exec_segment  -         0x800000      0x80dcfa      0x0       0xdcfa
compressed    NEGATIVE  0x800188      0x80d0c2      0x188       0xd0c2  provable data (53050 B)
