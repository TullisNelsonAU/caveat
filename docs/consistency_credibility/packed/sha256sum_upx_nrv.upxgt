# Format-exact GT for sha256sum_upx_nrv — derived from UPX's own b_info chain (NOT a disassembler).
# 3 b_info blocks; p_filesize=126648.
# field       label     vaddr_start   vaddr_end     file_start  file_end
exec_segment  -         0x800000      0x80587a      0x0       0x587a
compressed    NEGATIVE  0x800188      0x804c45      0x188       0x4c45  provable data (19133 B)
